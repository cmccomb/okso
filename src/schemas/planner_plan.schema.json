{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PlannerPlan",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["tool"],
    "additionalProperties": false,
    "properties": {
      "thought": {
        "type": "string",
        "description": "Short reasoning blurb about why the following tool and arguments are chosen.",
        "minLength": 1
      },
      "tool": {
        "type": "string",
        "minLength": 1,
        "description": "Name of the tool to invoke for this step. Use react_fallback to defer tool choice to ReAct."
      },
      "args": {
        "type": "object",
        "description": "Structured arguments for the tool. Leave empty for react_fallback entries when arguments are unknown.",
        "default": {}
      }
    }
  },
  "minItems": 1,
  "description": "Ordered list of high-level plan steps represented as objects with tool, args, and thought fields.",
  "examples": [
    [
      {
        "thought": "Explore tools interactively when the right arguments are unclear.",
        "tool": "react_fallback",
        "args": {}
      },
      {
        "thought": "Report findings for the user.",
        "tool": "final_answer",
        "args": {}
      }
    ],
    [
      {
        "thought": "Record the requested notes.",
        "tool": "notes_create",
        "args": {
          "thought": "Record the requested notes.",
          "title": "Research notes",
          "body": "The next delivery step for the research are..."
        }
      },
      {
        "thought": "Share the notes back to the user.",
        "tool": "final_answer",
        "args": {}
      }
    ]
  ]
}
