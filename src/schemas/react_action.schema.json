{
  "title": "ReactAction",
  "description": "Template ReAct action schema; per-tool oneOf variants are populated during compilation/generation.",
  "oneOf": [
    {
      "type": "object",
      "additionalProperties": false,
      "required": ["thought", "tool", "args"],
      "properties": {
        "thought": { "type": "string", "minLength": 1 },
        "tool": { "const": "final_answer" },
        "args": {
          "type": "object",
          "required": ["input"],
          "additionalProperties": false,
          "properties": {
            "input": { "type": "string", "minLength": 1 }
          }
        }
      },
      "$comment": "Illustrative example only; actual tools and schemas are generated dynamically."
    },
    {
      "$comment": "Per-tool variants are injected during schema compilation/generation."
    }
  ]
}
