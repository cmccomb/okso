You are completing missing fields for a tool call.
Return ONLY a JSON object for the tool args with all "${missing_token}" tokens replaced.

Tool:
${tool}

User request:
${user_query}

Plan outline:
${plan_outline}

History of Plan Execution So Far:
${history_text}

Planner notes:
${planner_thought}

Current args JSON:
${args_json}

Args schema:
${args_schema}

Update any placeholders based on the history context.