# CLI helpers

The CLI package owns argument parsing and stdout-facing helpers. Keep command-line parsing,
usage text, and presentation-focused utilities such as `output.sh` here so they remain
separate from core logic and planning concerns.
