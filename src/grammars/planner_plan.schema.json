{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PlannerPlan",
  "type": "array",
  "items": {
    "type": "string",
    "minLength": 10
  },
  "minItems": 1,
  "description": "Ordered list of high-level plan steps. Each item will be numbered downstream before execution.",
  "examples": [
    [
      "Use final_answer to respond directly to the user."
    ],
    [
      "Use note_create to jot down key points as requested by user",
      "Use final_answer to summarize for user"
    ],
    [
      "Use terminal to run tests",
      "Use terminal to review failures",
      "Use final_answer to report findings"
    ]
  ]
}
