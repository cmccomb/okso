# Sample environment configuration for okso assistant
# PLANNER_MODEL_SPEC (string): HF repo[:file] identifier for planner llama.cpp calls.
PLANNER_MODEL_SPEC=example/repo:demo.gguf
# REACT_MODEL_SPEC (string): HF repo[:file] identifier for ReAct llama.cpp calls.
REACT_MODEL_SPEC=example/repo:demo.gguf
# PLANNER_MODEL_BRANCH (string): branch or tag to fetch the planner weights from.
PLANNER_MODEL_BRANCH=main
# REACT_MODEL_BRANCH (string): branch or tag to fetch the ReAct weights from.
REACT_MODEL_BRANCH=main
# APPROVE_ALL (bool): true to skip confirmations by default.
APPROVE_ALL=false
# FORCE_CONFIRM (bool): true to always prompt before running tools.
FORCE_CONFIRM=false
# VERBOSITY (int): 0=quiet, 1=info, 2=debug.
VERBOSITY=1
# LLAMA_BIN (string): llama.cpp binary to use for scoring; can be a stub during testing.
LLAMA_BIN=./bin/llama-cli-mock

